module.exports = controller = {
    home : (res) => {
        res.write('home');
    },
    about : (res) => {
        res.write('about');
    },
    contacts : (res) => {
        res.write('contacts');
    }
}